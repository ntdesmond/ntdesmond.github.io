import styled from 'styled-components';
import { InTextSubHeading } from '../../../../components/typography/Headings';

const SkillCategoryHeading = styled(InTextSubHeading)`
  white-space: nowrap;
`;

export default SkillCategoryHeading;
