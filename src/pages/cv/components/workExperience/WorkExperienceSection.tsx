import { Section } from "../Section";

const WorkExperienceSection = (props: { children: React.ReactNode }) => (
  <Section title="Work Experience">{props.children}</Section>
);

export default WorkExperienceSection;
