name: Build Webpack and generate PDFs

# Build is done for all pushes except the master branch.
# In master, this workflow is called as a
on:
  push:
    branches-ignore: [master]
  workflow_call:

# Allow one concurrent build for each branch
concurrency:
  group: build-pages-${{ github.ref }}
  cancel-in-progress: true

env:
  PDF_OPTIONS: |
    {"format": "A4"}

jobs:
  build:
    runs-on: ubuntu-latest

    # Read-only permissions for GITHUB_TOKEN
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js 16
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        run: npm install

      - name: Build CV
        run: npm run build:cv

      - name: Serve CV
        run: npm run preview -- --host & npx wait-on tcp:4173

      - name: Generate PDF file (en)
        uses: ntdesmond/html2pdf-action@v1.1.1
        with:
          path: http://172.17.0.1:4173/cv.html?language=en&print
          output: ./src/pages/cv/files/cv_en.pdf
          options: ${{ env.PDF_OPTIONS }}

      - name: Generate PDF file (ru)
        uses: ntdesmond/html2pdf-action@v1.1.1
        with:
          path: http://172.17.0.1:4173/cv.html?language=ru&print
          output: ./src/pages/cv/files/cv_ru.pdf
          options: ${{ env.PDF_OPTIONS }}

      - name: Build the rest of the app
        env:
          GITHUB_REPO: ${{ github.repository }}
        run: |
          npm run build:main

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: "build/"
